import React from 'react';
import './spinner.css'; // Імпортуємо CSS файл

const Spinner = () => {
    return (
        <div className="loader"></div> // Спінер, який відображатиметься
    );
};

export default Spinner;
